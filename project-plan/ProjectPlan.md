# Overview

* We have 1 sample patient record file based on FHIR r4 standard `inputdata/scenario1_fhir_bundle[78].json`
* We need to reconcile the data for 

# Assumption

* This feature is a one of the modules in a much larger data processing pipeline
* Data processing can happen Default (auto-mode) or HIL (Human-in-Loop, manual-mode)

# Goals

## Must Have (MVP)

* Default (auto-mode)
  * loads the sample JSON
  * FE sends for processing
  * BE sends back evaluation report
  * FE to organize display each patient record and its completeness based on evaluation report

## Good to Have (stretch goal)

* HIL (Human-in-Loop, manual-mode)
  * upload custom JSON (must adhere to FHIR r4 patient record conventions for processing to work)
  * FE sends for processing
  * BE sends back evaluation report
  * FE to organize display each patient record and its completeness based on evaluation report
  * user can expand sections and make selective modifications to complete/clean the incomplete data.
  * use has the option to download the autogenerated JSON or customized JSON



